const albumDatabase = require('./album-database');
const listDatabase = require('./list-database');
const songDatabase = require('./song-database');
const userDatabase = require('./user-database');

module.exports = {
  albumDatabase, listDatabase, songDatabase, userDatabase,
};
